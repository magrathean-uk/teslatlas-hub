# Next /goal — prepared, not started

Open `/Users/bolyki/dev/source/teslatlas-service` and select **GPT-5.6 Sol / Max**.
The following text is unsent; preparing it has not created a goal.

```text
/goal Finish the Teslatlas Hub ecosystem's installed lifecycle and reproducible source/package handoff on Debian ARM64 and Apple silicon, preserving APP_V7_ADOPTION_READY.

Read WORKSPACE_AUTHORITY.md and docs/development/{MASTER_PLAN.md,NEXT_PHASE_PLAN.md,TASKS.json,COORDINATION.md,ENVIRONMENT.md}, then the selected product PLAN.md and STATUS.json. Use HISTORY_REVIEW.md and receipts only for relevant evidence. G0–G7 and bounded Home Assistant Container lifecycle are already accepted; do not restart the old reset programme.

Execute L1–L3 in NEXT_PHASE_PLAN.md. Start with one Debian ARM64 Hub package/service journey: ordinary installation, setup, pairing, current/history, restart, identity/config/data retention, backup/restore, supported upgrade/rollback and data-preserving removal. Identify an exact predecessor before claiming upgrade/rollback. Then optional Edge and changed package-to-consumer boundaries. Prove supported Apple-silicon installed lifecycle, requiring separate sequential Hub macOS 13 and Swift macOS 14 receipts with at most one Mac guest at a time; accepted macOS 27 runs prove neither floor. Complete reproducible local source/package handoffs and newly affected TS, Swift, Protocol or HA replacement-upgrade assertions. Fix observed failures; avoid unchanged suite reruns.

Use GPT-5.6 Sol / Max to coordinate, Sol / High for implementation and independent review, Luna / Max for bounded read-only exploration. No fast mode or silent substitution. Default to at most two useful subagents with exact deliverables and owned paths, one writer per repository. No persistent product tasks or archived queues. Hub owns runtimes; one heavy build/VM installation under the shared lab lock.

This goal authorizes L1–L3 implementation, focused tests, local development package installation, fresh isolated synthetic journeys and needed lean ARM64/Mac VM work. Reuse the retained Debian guest after verifying ownership/access; at most one Mac guest when a gate needs it and disk budgets permit. Verify cleanup and stop idle guests. Reuse valid source evidence; issue fresh credentials, identities and handoffs. Never reopen a closed cohort.

Do not inspect, edit, build, test, clean or delegate anything in app/ or its tasks/tooling. Viewer remains deferred with no worker, dependency or gate. x86/x86_64/amd64/Intel/Azure remain paused. Preserve dirty trees, original receipts, repository-roots Git storage and private data. No reset, clean, stash, branch, worktree, commit, push, tag, release, CI, signing/notarization, production deployment or Tesla account/vehicle action. The previous one-off upload is not ongoing publication authority. Keep automation paused.

D1 named-source parity is a separate input-dependent gate: use only a fresh owner-supplied read-only export with provenance. Do not inspect old excluded fixtures or claim synthetic results prove real parity. Continue independent work while an image, predecessor package or input is unavailable; name the missing input and affected assertion.

Update plans/status at material transitions. Separate source, package, installed service, synthetic and named-source proof. Keep RESULT / EVIDENCE / NEED / NEXT updates concise. Deliver exact artifacts, reproducible commands, acceptance receipts and limits. L1–L3 must pass to achieve the objective; report externally blocked mandatory assertions honestly without marking them accepted or complete. Do not start App or Viewer work.
```
