# Start authorization — full-solution goal draft

Revision: 2026-09-19. The owner corrected the desired outcome to a fully working Hub
and all active dependencies. The earlier instruction to write the goal without
sending it remains: this task updates planning and the authorized source uploads only.
No goal, implementation, build, runtime, VM or automation is started here.

[GOAL_PROMPT.md](GOAL_PROMPT.md) now covers all F0–F7 in
[NEXT_PHASE_PLAN.md](NEXT_PHASE_PLAN.md), not only L1–L3 or App adoption. The accepted
G0–G7/HA evidence remains valid within its original scope; full completion is pending.

When the owner sends the goal, it authorizes focused implementation and tests,
isolated package/runtime acceptance, lean VM work under ENVIRONMENT.md, and necessary
validated source commits/pushes and exact dependency catalog updates in the existing
GitHub repositories. The source-only publication authority is explicit in the prompt.
No force push, reset, clean, stash, discarded dirty work, CI, binary release/asset,
tag, signing/notarization or production deployment is authorized.

F5 named-source/parity/passive evidence is required. Prepare concrete work and request
a fresh named read-only export/capture or precise authorization when needed. Do not
reuse historical private roots/credentials. Do not access Tesla accounts, expose
public ingress, collect real telemetry or send vehicle actions without separate
explicit owner authority. Complete independent work while required inputs are missing;
never mark the entire solution complete without every mandatory acceptance gate.

Sol/max coordinates; Sol/high implements/reviews; Luna/max explores. No fast mode.
One writer per repo, Hub owns shared runtimes, one heavy job under the shared lock.
All six products are mandatory, including HA. App/Viewer and x86/Intel/Azure stay
excluded. SDK support tests use only SDK-owned isolated harnesses. Keep automation
paused and archived tasks/cohorts closed.
