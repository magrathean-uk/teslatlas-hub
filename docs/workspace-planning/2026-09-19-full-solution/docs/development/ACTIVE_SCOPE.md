# Active scope — full working ecosystem planning

Revision: 2026-09-19, owner clarification. G0–G7 and bounded HA Container lifecycle
are accepted intermediate milestones; **the full solution is NOT_ACCEPTED**.
This task amends the complete programme and unsent goal, preserving the current
source-upload request. It starts no implementation, build, runtime or native goal.

All six products are required: Hub, Protocol, TypeScript SDK, Edge, Swift SDK and
Home Assistant. Optional installation does not make a product optional for completion.
The full F0–F7 programme includes every supported active feature, ordinary source/
native/container installation and lifecycle, SDK floors/consumers, HA installation/UI/
recovery, exact source bootstrap/catalog, fresh real-data/parity evidence and final
combined installed acceptance. See [NEXT_PHASE_PLAN.md](NEXT_PHASE_PLAN.md).

Debian ARM64 and Apple silicon remain active. SDK-owned iOS evidence is included
where needed for existing SDK support claims, without App access. App and Viewer
stay excluded, including their source/plans/tasks/tooling. x86/amd64/Intel/Azure remain
paused. No supported active feature may be silently removed just to accept the goal.

Sending [GOAL_PROMPT.md](GOAL_PROMPT.md) authorizes the described development and
source-only GitHub publication/catalog updates, not CI, binary releases, tags,
signing/notarization or production. Real account/collection/public-ingress/vehicle
actions require fresh explicit authority. Missing named inputs keep F5 and the full
goal incomplete while independent work continues. Keep automation paused.
