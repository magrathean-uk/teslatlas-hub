# Full-solution planning snapshot

This replaces the earlier L1–L3-only endpoint with all mandatory F0–F7 across Hub,
Protocol, TypeScript, Edge, Swift and Home Assistant. App readiness remains an accepted
intermediate milestone. The goal is written but not sent or started.

The parent workspace has no Git repository. Paths in SNAPSHOT_MANIFEST.json are
relative to that workspace, alongside the six product checkouts. Review and merge
these files there without overwriting newer work. Links keep their original workspace
context. The earlier ../2026-09-19 snapshot retains older archived history; this one
adds current planning and the pre-expansion archive. No App/Viewer content or raw chat
transcript is included. Product PLAN/STATUS files live in their own repositories.

UPLOAD_STATUS.json is captured before this documentation commit exists; the live root
copy records final remote hashes after the six pushes. Historical runtime receipts and
tested source identities are unchanged. This is planning, not new runtime acceptance.
