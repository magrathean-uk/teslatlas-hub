# Start authorization — 2026-09-18 reset

The owner requested a clean reassessment, new plans, closure of old product tasks,
lean VM reconstruction and a new Sol 5.6 coordinator prompt.

Preparation in this task: archive old product chats, pause their coordinator schedule,
preserve old plans/evidence, refresh all canonical plans and write the handoff.
The new task starts execution when the owner sends [HANDOFF_PROMPT.md](HANDOFF_PROMPT.md).
Do not reopen retired tasks or consume their old queued instructions.

Already authorized for that execution: focused source changes and tests, bounded
subagents, shared Debian ARM64 and needed Apple-silicon Mac VM reconstruction,
verified reproducible artifact cleanup, isolated synthetic/local runtime tests and
fresh Hub-owned consumer handoffs. Use [ENVIRONMENT.md](ENVIRONMENT.md) for limits.
Do not ask again merely because old files say no new VMs or specify old models.

Coordinator: `gpt-5.6-sol` / `max`; implementation/review: `gpt-5.6-sol` / `high`;
exploration: `gpt-5.6-luna` / `max`. No fast mode. Respect actual configured model
capability; report inability instead of silently substituting.

No App or Viewer work, no x86/Intel work, no production/vehicle actions, no commits,
pushes, releases, CI or broad destructive cleanup. Preserve dirty source and evidence.
Archived native goals remain historical; do not claim they were completed or changed
through archival. Track this programme in TASKS.json; create no new native goal unless
explicitly requested under the native goal tool's rules.
