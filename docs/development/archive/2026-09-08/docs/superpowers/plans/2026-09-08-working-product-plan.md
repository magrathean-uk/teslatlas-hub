# Teslatlas Hub Working Product Implementation Plan

**Latest user model policy (2026-09-08):** This coordinator and delegation use `gpt-6-astra` with `thinking=high`. This product task, coding, goal execution and all development workers use `gpt-5.6-sol` with `thinking=medium`. This supersedes every earlier model instruction in this plan and its historical goal snapshot. Preserve checkpoints at model transitions and verify the actual new turn model.

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete the remaining Hub working-product scope from the current dirty source checkpoint: finish every source-fixed adapter and Hub product seam, pass all 21 installed cells, 444 case slots and ten cohort requirements on the three required target classes, report history/passive evidence separately, validate Docker and current operator documentation, and finish the authorized source-only public-catalog/GitHub phase without weakening any gate.

**Architecture:** Retain the existing Rust service, SQLite store, AppKit manager, public HTTP profile, companion bootstrap, source-fixed adapter registry and installed acceptance supervisor. Complete each adapter through the existing `FixedInstalledAdapter` interface, keep controller/runtime observations runner-owned, and treat local source checks, installed acceptance, passive data and publication as separate evidence layers.

**Tech Stack:** Rust 1.98+ and Cargo, SQLite, Swift/XCTest and AppKit, Python 3 integration tooling, Node.js/Playwright, native LaunchAgent/systemd packaging, and source-built Docker Compose.

**Spec:** Workspace `WORKSPACE_AUTHORITY.md`, `docs/superpowers/specs/2026-09-05-hub-ecosystem-compatibility.md`, `docs/superpowers/plans/2026-09-05-hub-ecosystem-compatibility.md`, Hub `docs/compatibility/{matrix.json,execution-state.json}`, and [the Docker product plan](2026-09-08-docker-product.md).

## Global Constraints

- Work in the existing independent `main` checkouts and preserve unrelated dirty changes. Do not branch, reset, clean, stash or overwrite another owner's files.
- Hub owns Hub service/controller/runner code, Hub-side adapter modules, aggregate evidence and workspace coordination. Sibling owners own their product source and contract publication. Teslatlas App is excluded.
- GitHub is source storage only. Do not add CI, create releases or tags, upload artifacts, or publish images. Commit/push and public-catalog writes still require their explicit final-phase authority.
- Keep production services, vehicles and the quarantined Debian ARM64 guest untouched. Installed work uses fresh isolated targets and private redacted inputs; no vehicle wake or command is an acceptance shortcut.
- Do not weaken strict contract, provenance, runtime, cleanup, profile or matrix completeness checks to turn unavailable evidence into a pass.
- The ecosystem coordinator uses `gpt-6-astra` with reasoning `high`. This Hub product task and every Hub implementation, verification, review and development worker use `gpt-5.6-sol` with reasoning `medium`. Historical model clauses returned by `get_goal` or retained in old briefs are superseded and must never select the worker model. Verify each dispatch against `hub/AGENTS.md`.

---

## Durable execution checkpoint — 2026-09-08T18:02:04Z

The machine-readable checkpoint is [`2026-09-08-goal-checkpoint.json`](2026-09-08-goal-checkpoint.json). A fresh native goal read on 2026-09-08 reports the goal `active` (`tokensUsed=10728942`, `timeUsedSeconds=23201`, `createdAt=1788848176`, `updatedAt=1788892202`, `remainingTokens=null`). Any retained `gpt-5.6-luna` wording in its historical objective is stale and does not override the current Astra/Sol policy above. This documentation update did not mutate the native goal state. The checkpoint remains the durable milestone and resume reference and has no token or elapsed-time budget.

**Current acceptance:** `0/21` installed cells, `0/444` required case slots and `0/10` cohorts are accepted. Passing source suites, a package build, an adapter review or a registry row does not change those counts.

**Resume rule:** Start with `hub-swift-and-sibling-registry`. After each gate, update this checkpoint and `docs/compatibility/execution-state.json`, then continue to the next ready milestone in the same goal. A missing host or owner input blocks only the dependent lane; continue other ready work. Do not finish the goal after one successful tranche or mark it complete while any mandatory cell, case, cohort, final evidence review, Docker claim, documentation claim or authorized publication gate remains open.

### Durable milestones

| Milestone | Status | Owner task ID | Owned files and interfaces | Verification and exit |
| --- | --- | --- | --- | --- |
| Shared runner and TypeScript source base | **DONE** | `01a07f89-45cb-7ea2-b96e-aa89de18fd14` | `tools/interop/matrix_runner/{installed.py,installed_registry.py,adapter_wire.py,docker_pipe.py,process_generation.py,receipt_validation.py,typescript_installed.py}`, `tools/interop/client_lanes/`, and focused tests. Produces the reviewed `FixedInstalledAdapter` dispatch, runner-owned stop/runtime admission and the fixed `typescript_node`/`typescript_browser` rows. | Existing I1-I18 rereview and current local suites are recorded below. Exit is source-only: installed acceptance remains `0/21`. |
| Swift and remaining sibling registry | **IN PROGRESS** | `01a07f89-45cb-7ea2-b96e-aa89de18fd14` | Finish `tools/interop/matrix_runner/swift_installed.py` and `test_swift_installed.py`; update `installed_registry.py`, `test_installed_registry.py`, `run.py`, `test_run.py`; add focused Hub-owned modules/tests for Protocol, Home Assistant, Viewer and Edge. Every module must supply `build_session_input`, `launch_adapter`, `admit`, `build_supplement`, `execution_logs` and `runtime_inventory` for a source-fixed `FixedInstalledAdapter`. | The registry currently has the two reviewed TypeScript source rows and a Protocol source row whose final exact bundle still needs independent Hub review. Swift, Home Assistant, Viewer and Edge Hub modules remain fail closed until their named worker/runtime, browser-origin/supplement, path-map or root-owned fixture/launch-inventory inputs are bound and reviewed. Run affected adapter, registry, shared runner and sibling contract suites; obtain current independent source review. Exit when all seven client entries are operational at source level, with no installed pass claimed. |
| Hub native product, lifecycle and bootstrap closure | **READY** | `01a07f89-45cb-7ea2-b96e-aa89de18fd14` | Tasks 1-4 below: `src/`, `tools/interop/installed_hosts/`, `tools/companions/`, native packaging/scripts, AppKit code and `docs/compatibility/execution-state.json`. Consumes the fixed runner and produces reviewed native packages, lifecycle/recovery proof and a repeatable local-candidate bootstrap. | Close only current findings, run targeted tests then required Rust, packaging, bootstrap, repository-layout and provenance gates. Exit with reviewed macOS and Debian package candidates and local source evidence; host installation remains the next milestone. |
| Existing Mac and VPS validation | **WAITING ON SOURCE/CANDIDATES** | `01a07f89-45cb-7ea2-b96e-aa89de18fd14` | Current Mac ARM64, dedicated local Colima infrastructure, and the existing VPS test surface under its established safety and action authority. Consumes all fixed entries and exact reviewed candidates before any Azure request. | Complete applicable Mac native/package/browser/Docker and VPS read-only status/log/database plus already-authorized isolated tests. Preserve production services, protected inputs, the quarantined ARM guest and vehicle state. Record actual results and residuals; local/container evidence does not change installed counts by itself. |
| Final Azure Debian cross-architecture acceptance | **WAITING FOR FINAL OWNER RESOURCE** | `01a07f89-45cb-7ea2-b96e-aa89de18fd14` | Owner-provided fresh Azure Debian ARM64 and x86_64 VMs, private matrix configuration/receipts, `tools/interop/run.py` and `docs/compatibility/{matrix.json,execution-state.json,acceptance.md}`. | Azure is requested only after development/integration and Mac/VPS tests are finished. Hub then coordinates the exact same candidate identities on both architectures for bootstrap, `.deb`, Docker and companion installed integration: fresh install, startup, function, restart/persistence, cleanup, and applicable upgrade/uninstall. Run the complete matrix with runner-owned final-stop/child-zero proof. Exit only at `21/21`, `444/444`, `10/10`; history import and passive Fleet/Edge outcomes remain separate named results. |
| Docker, documentation and source-only public finish | **WAITING FOR FINAL ACCEPTANCE/AUTHORITY** | `01a07f89-45cb-7ea2-b96e-aa89de18fd14` | Final phases 1-4: `Dockerfile`, `compose.yaml`, `packaging/docker/`, current Hub docs/README/provenance, aggregate compatibility/catalog files and explicit task-owned Git paths. Consumes accepted native/source identities and sibling commits. | Local Docker work may proceed on the dedicated Colima context, but claimed Debian ARM64/x86_64 Docker acceptance belongs to the final Azure gate. Public catalog needs exact reachable sibling commits; commit/push needs explicit Git-write authority. Reconcile docs to accepted evidence, review the final diff, then perform and verify only an authorized source publication. No release, tag, binary/image upload or deployment authority is implied. |

### Dependency and unblock table

| Owner task ID | Exact missing input | Work that can proceed now | Unblock event |
| --- | --- | --- | --- |
| `01a07f89-45cb-7ea2-b96e-aa89de18fd14` (Hub) | No Swift owner input is missing: the reviewed Swift contract, validator, raw schema, coordinator and worker source bundle are present. Hub still lacks SessionInput staging, fixed workers, lifecycle hooks and the registry row. | Complete and review the Hub-owned Swift seam first. Protocol, HA, Viewer and Edge current contract/coordinator files can be independently bound next. | Focused Hub tests and independent source review accept each complete fixed entry. |
| `01a07f89-48b6-7143-919f-8b389fbfb1ec` (Protocol) | The source row is registered, but its final exact v2 manifest/validator/raw-schema/adapter identity still requires independent Hub review. | Keep admission fail closed while reviewing the final owner bundle and registry callables. The owner's dedicated offline ARM64 container gate is local evidence only. | Hub independent review accepts the exact stable source set and fixed entry. |
| `01a07f89-54c0-75c0-bcd2-5efc87b408bf` (Home Assistant) | No new source request is needed. `docs/hub-admission-handoff-2026-09-08.json` supplies the contract, four raw schemas, 31-member component identity, launcher/wire hashes and G1/G1/G2 plus close-barrier rules. | Hub implements the HA stage/launch/admit/log/runtime callbacks and tests. | Hub review accepts the exact handoff; installed Python 3.14.2/HA 2026.8.3 remains a later host input. |
| `01a07f89-5148-7463-b374-7b00232e082c` (Viewer) | No new source request is needed. The 24-case/two-actor contract, validator, two raw schemas, `matrix-live.mjs`, 81-member SDK binding and packaged Viewer source are present. | Hub implements the Viewer fixed entry and verifies the normal package/Playwright command without treating its pending semantic placeholders as acceptance. | Hub review accepts the exact owner bytes and a composed source-level lane. |
| `01a07f89-57a9-7831-9162-c1dede446ee9` (Edge) | No new source request is needed. The Edge handoff supplies the 16-case, six-actor, 31-phase contract/coordinator/schema set. Hub still lacks the root-owned fixture and launch inventory. | Hub implements the reviewed Edge fixture/controller seam and fixed registry callbacks without touching the quarantined guest or a vehicle route. | Hub review accepts the fixture and one composed isolated source/local-process lane. |
| `01a07f89-45cb-7ea2-b96e-aa89de18fd14` (Hub) | Completed source/integration work, exact reviewed candidates, private inputs and completion of all applicable Mac and VPS tests. | Finish source adapters, configs, candidate review and existing Mac/VPS gates now; retain exact pending rows. | Development/integration and Mac/VPS evidence are complete, so the owner can create and provide the final Azure VMs. |
| `01a07f89-45cb-7ea2-b96e-aa89de18fd14` (Hub) | Owner-provided fresh Azure Debian ARM64 and x86_64 VMs after every earlier gate is done. | Prepare a concrete, identity-bound two-architecture run card; do not request, provision or substitute Azure early. | Owner supplies both final VMs and access; Hub admits the exact candidates/private inputs and executes the coordinated final v1 matrix. |
| `01a07f89-45cb-7ea2-b96e-aa89de18fd14` (Hub) | Exact reachable sibling commits and the existing reviewed final-phase source-write gates. | Use the running dedicated Colima context for serialized local evidence and reconcile documentation/static Compose/provenance checks without claiming final runtime or publication. | Both-architecture final acceptance passes; sibling commits are reachable; the concrete source update passes its existing final-phase authorization and review gates. |

### Current resource and ordered test checkpoint

The dedicated `colima-interop-20260905` context is running as local isolated infrastructure (`linux/arm64`, daemon `eab1656e-82ca-477b-bdf6-08d34120524f`, four CPUs, about 8.3 GB RAM). It is not an installed cell or proof of Debian ARM64/x86_64 product support. Preserve the 23 GB free-disk scheduling guard recorded for this work and serialize heavy image builds. Protocol used the first slot for a frozen, network-none ARM64 source image and reported 133 passing tests; that is offline container evidence only. Swift owns the next heavy slot for its frozen-source supported-Linux package gate. Viewer, Home Assistant, Edge and Hub heavy builds wait until that slot is explicitly released. Use `DOCKER_CONTEXT=colima-interop-20260905` or `docker --context colima-interop-20260905`; do not change the global context or disturb retained workloads and volumes.

Execute the v1 path in this order:

1. Finish all Hub and sibling development, strict source registration, independent source review, native lifecycle/package work and local integration. The current registry has two TypeScript rows; Protocol is source-registered pending independent review; Swift, Home Assistant, Viewer and Edge remain fail closed on their named runtime/root wire inputs.
2. Complete every applicable existing Mac test, then VPS tests under the already established VPS safety and action authority. Start with read-only status/log/database evidence and use only already-authorized isolated actions. Do not wake a vehicle, alter protected or quarantined resources, or disturb production services.
3. Freeze and independently review the exact Hub, `.deb`, Docker and companion candidate identities plus the private run inputs. Record every remaining Mac/VPS limitation before asking for external resources.
4. Only when steps 1-3 are complete, ask the owner to create and provide fresh Azure Debian ARM64 and x86_64 VMs. Azure cannot replace an earlier Mac/VPS test or justify deferring it.
5. On both owner-provided Azure architectures, Hub coordinates the same exact candidates through bootstrap, `.deb` and Docker verification plus all applicable companion installed integrations. For each architecture record fresh install, startup/readiness, required function, restart and persistence, cleanup and child-zero proof, and applicable predecessor upgrade and uninstall/data-preservation behavior. Then run the authoritative complete receipt gate and accept only exact `21/21`, `444/444`, `10/10` results.
6. Reconcile documentation and evidence to actual outcomes, complete independent final review and use the existing explicit source-write gate for any public catalog/GitHub update. Azure provisioning and successful tests grant no release, tag, binary/image upload, deployment or publication authority.

**Real next action:** Complete and independently review the active Hub source-fixed entries, starting with the Protocol row's exact final source binding and the remaining Swift/Home Assistant/Viewer/Edge fail-closed seams. Run the Swift frozen-source Linux package check in the next exclusive heavy Colima slot while host-local source work continues. Do not request Azure until development/integration and every applicable Mac/VPS gate are complete.

> **Execution record:** The user authorized implementation on 2026-09-08. The original audit and unchecked rows remain useful planning context; this record captures the bounded Hub execution completed in the current dirty `main` checkout. It does not authorize publication, releases, or live-service changes.

**Status:** Planning checkpoint refreshed; Hub controller/runner corrections, native Fleet listener separation, core Docker packaging, documentation wiring, and the Hub-owned TypeScript Node/browser installed-lane source are implemented and locally verified. Installed-host, live-service, full ecosystem, Docker runtime and publication gates remain pending.

**Detailed audit:** Revised after read-only source/report comparison on 2026-09-08. The execution update below is historical evidence; the durable checkpoint above is the current work queue.

## Execution update — 2026-09-08

The authorized Hub work preserved the existing independent `main` checkouts and unrelated dirty files. It added the fixed loopback Fleet listener alongside public TLS, bounded controller/session cleanup and journaling, deadline-aware installed matrix supervision with descendant cleanup, package-safe interop imports, the Hub-owned TypeScript Node/browser installed lane, and source-built core Docker packaging plus operator documentation. No commit, push, release, CI, artifact upload, service restart, Docker runtime, SSH, vehicle command, or live-service operation was performed.

Verified evidence includes full Hub Rust tests (`904` library, `64` binary, `2` Edge E2E, `2` interop, `2` TLS E2E, and `3` doc tests passed; `2` ignored), `cargo fmt --check`, `cargo clippy --locked --all-targets --all-features -- -D warnings`, `132` installed-host tests, `176` interop tests, Docker static packaging checks, repository-layout checks, provenance checks, and `git diff --check`. The focused Fleet listener and matrix correction suites also passed. A focused current-source rereview of shared findings I1 and I3 passed at import/composition level; its I16 partial finding led to a current Node/private-broker challenge-chain correction. The correction validates nested SessionInput, broker proof/descriptor/invitation shapes and bounds, synchronizes the private broker challenge chain with the guest session, and requires Node proof challenges to match the broker request. The current lane has 28 combined Node tests (9 strict installed-lane tests), 22 Python session tests for the challenge chain, and the full 176-test Hub interop suite passing. The independent rereview of the exact ten-file source bundle is recorded in `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-task10-shared-i16-fix/independent-rereview.md`: no material source residual remains within the reviewed I16 scope. The fixed installed registry now has two Hub-owned, source-bound TypeScript entries; this is a source/integration gate only and does not claim installed capability or matrix acceptance. Earlier Home Assistant verification passed 61 product tests and 49 matrix/live tests; the latest owner handoff records 115 full-suite passes, 2 live skips and Ruff pass. The shared continuation also exercised the HA initial/final barrier, actual operation-record journal parsing, redacted supplement rejection, strict Node wire/latch behavior and capped nonblocking Docker pipe with `36` matrix-runner tests, `3` Docker bridge tests and `28` Node client-lane tests (the configuration-less `run.mjs` module invocation is not a lane run). The non-App AGENTS coverage record is `task-10-agent-guidance-inventory.md`. The Docker daemon was unavailable, so container build/runtime acceptance remains unperformed. Installed Linux/macOS host rows, remaining sibling adapters, full live Fleet/Edge acceptance, and final source publication remain pending in `docs/compatibility/execution-state.json`.

The companion bootstrap source gate now passes all 66 tests across its five test files; a Python 3.9 transport-timeout classification defect was corrected in `tools/companions/cli.py`, with stdout retained at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-companion-bootstrap/companion-tests.stdout` (SHA-256 `951ccd46abc08374627ac2a38f02131c97a9ca125d4058688cec2ea79faa4967`). This is source-level regression evidence only; installed cohort and public catalog acceptance remain pending.

A read-only six-source local manifest was also generated from the current sibling checkouts for candidate binding: `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-companion-bootstrap/local-sources-current.json` (SHA-256 `fc663b9895af9bca4039d68c85373fc244c2ad62955cb18d168a8582ad7a66c1`). It records current dirty-source identities only and does not promote any source to public or installed support.

The current candidate-file layout and provenance gates pass for 507 files using private candidate manifests. The final source manifest is `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-companion-bootstrap/current-hub-candidate-source-manifest-post-i14-i15-final.json` (SHA-256 `3bf0fe668d89cc8ce3820dc0db1c672bee09e28a2aceceaa0a8f4a0357db2e6e`). The tracked-index layout check remains pending because `docs/guides/install-docker.md` and the Docker/integration additions are intentionally unstaged; no staging or publication was performed.

A fresh read-only runtime snapshot is retained at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-working-product/current-runtime-inventory.json` (SHA-256 `6f396f9029bbe785ddfb4ef2f095aed21a3e8254846ee02dfebd7f72388c45dc`). It confirms the prepared macOS/ARM64/AMD64 matrix guests are stopped, package-free and not registration-ready; the protected macOS observation remains running; the quarantined ARM guest was untouched; the current AMD64 Lima port is `54518`, ARM64 is `57067`; and the Docker daemon is unavailable. The current AMD64 package candidate (`5025383ca2053ea47c4d039f4719084ea2c9b9896c525cf91aef247642df80d8`) and macOS package candidate (`3a8af8985713502b6c772adcf045ce41b966f68b1463e654f827f5f19368b6f4`) remain review-pending inputs, not installed acceptance.

The Docker operator path now mounts the private repository-root `config.toml`, protects it and `tls/` through `.gitignore`, and keeps the checked-in template out of runtime configuration. The focused Docker packaging gate passes after this correction; Docker daemon/runtime acceptance remains pending.

A current read-only Protocol admission audit confirms that its source gate passes (`./tools/check`: 119 tests; three profiles, 31 runs, 31 passed) but the adapter cannot yet enter the fixed installed registry. The current dirty Protocol source remains the approved v1 stdout adapter: `conformance/hub_matrix.py` accepts only `schema_version: 1`, emits normalized JSON with `print(...)`, and closes its broker in the existing `finally` path; it has no v2 `SessionInput`, file-bound normalized/actor evidence, or runner close-ack lifecycle. The reviewed `tools/matrix-contract.json` and pure `tools/matrix_contract.py` publication files are absent. At this pre-TypeScript audit snapshot, `FIXED_INSTALLED_REGISTRY` was empty; the separate Hub TypeScript entries added later are recorded under the continuation below. The audit is retained at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-protocol-registry-audit/current-audit.json` (SHA-256 `dc30738798aa97063b1df8c04c201b1082f75a0c4d7081d859ee8d386804427c`). This records a precise owner-bound integration blocker; no registry entry, sibling edit, host/VM/Docker operation, or installed acceptance was performed.

The fresh read-only current adapter inventory is retained at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-current-adapter-inventory/current-adapter-inventory.json` (SHA-256 `132e0cc65237b447d9c1b622f147da4667cac83e246b1cd34f6d370e7a7f1bc4`). It binds the current sibling checkout states and contract-file presence: Protocol remains a v1 stdout adapter without the reviewed v2 SessionInput/file-evidence/runner-close-ack surface; Swift has a contract bundle and its current 23-test source gate passes, but its earlier nine-Important-finding rejection has no current independent closure or worker/runtime result; Home Assistant correction 3 passes the current semantic source review and its current matrix/product source gates (49 + 36 tests) pass, but provenance and installed/runtime admission remain pending; Viewer and Edge have no current contract publication files. This inventory predates the separate Hub TypeScript binding: its statement that `FIXED_INSTALLED_REGISTRY` was empty is historical. The current registry has only `typescript_node` and `typescript_browser`; the authoritative matrix remains 21 cells, 444 case slots and 10 cohort requirements, with 0/21 cells accepted because installed runtime evidence is still absent. No adapter row, host/VM/Docker operation, or publication was performed.

The requirement-by-requirement audit is retained at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-current-adapter-inventory/completion-audit.json` (SHA-256 `975a598734425939db46d63712b73d9e76f91413ed0c10b20abe3fbe50576319`). It records source-level gates that pass separately from mandatory installed/runtime/publication gates that remain pending: accepted cells/slots/cohorts are `0/21`, `0/444`, and `0/10`. The quarantined ARM guest, production services and vehicle routes remain untouched.
A fresh independent Home Assistant correction-3 review now binds the exact current ten-file adapter bundle to checkout `f650331a1af0cf33cec271bc7eefc0f1201ebd4e` and the current 31-file component bundle. Its JSON review is `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-ha-current-review/current-independent-review.json` (SHA-256 `eec781894f5d31194d93c5c0797bfa30ea44c65baee53fd0b4bb5ca117222bfc`), with Markdown companion SHA-256 `9de747e35bfe00bc3ec905451e0d7bd7612b8affd39be154765c3ccb3bf10318`; source and component manifest hashes are `bff35f1342e4bb262dd9e06599695139df53631f613d38d48bcd698185eeb7c7` and `d8993f901c7b4733fa2c3e3692125d6fceda0e1bbb5ec115f0b9e73a69f6d845`. The review records semantic/provenance PASS, adapter `49 passed, 2 skipped`, product `111 passed, 2 skipped`, Ruff check PASS, and the existing whole-tree format warning (five files; no mutation). It does not establish a pinned HA/Hub runtime, installed lifecycle, Docker/UI/transport cleanup, or registry admission; this review predates the separate TypeScript entries, which remain source-only.
The current adapter inventory and completion audit were refreshed to bind that review: inventory SHA-256 `132e0cc65237b447d9c1b622f147da4667cac83e246b1cd34f6d370e7a7f1bc4`, completion-audit SHA-256 `0d9bf147b489bb0391755b0774dd478aa76b6e6763999822bc0007a5b796ff74`; accepted cells/slots/cohorts remain `0/21`, `0/444`, and `0/10`.
The private provenance/launcher handoff is retained at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-ha-current-review/provenance-launcher-binding.json` (SHA-256 `45ddef5ccde8503137b3781e3b8fafd7fba3aafd54cb884fb267d901403c606e`). It binds the HA contract/validator, exact SessionInput invariants, module and output identities, startup/barrier/cleanup order, and pending boundary; it contains no fabricated SessionInput, acknowledgement, registry mutation, or live claim.
A current TypeScript SDK candidate snapshot now supplies strict Node/browser contract manifests, pure validators and raw schemas. The exact current files in `teslatlas-sdk-typescript` at HEAD `b1cd548fef7ddd26be2637c14bb435480166cef7` have Node manifest/validator/raw hashes `7800f75b03e046f53e56b7356fac95089aae73ab7719e82ead88f39e45ae3299`, `600e1cc363d309e163b87e19498e1daf4ba309c325aeb8402513168ff1c12ef2`, `5a4ad38caa2dee8432eef0e8dd2f7c00b92db0aa3946eb2dab93c4fc13ad6908` and browser manifest/validator/raw hashes `3a04e33415330d2d3ce53d489841f0a28b768d982d2af5f255323d2f0cf6acb9`, `784d1618efdc6739248426d7ee6538772a3560c1569c903f75fb2af72755bf92`, `638feee0156286a9103148ed775ee126bbdc198f9a8e21210652c1871704415c`. Its exact-toolchain rebind passed clean `npm ci` plus the full format/lint/typecheck/protocol, 271 unit, 25 conformance, 52 protocol and pack/consumer verification under Node 26.7.0/npm 11.19.0 without `--force` or an engine override. The Hub now binds those exact contracts to fixed Node/browser entries and the reviewed 81-member SDK archive at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-working-product/sdk-m1-node-26.7.0-pack/teslatlas-sdk-2026.36.2.tgz` (SHA-256 `03ddddf132185056d60a490bc5237b3f6213d8e212209cfe111be5e09cf0a75c`), alongside owner-only staged inputs, a v2 descriptor and runner-owned runtime/close evidence. The SDK path/content witness remains `4b1a44f7d2cd0ab6c772642125358703aa25c1efe32d164a119a6fc9d858ded6`. The expected owner-staged 0600 manifest is `480226f91ef676461b171b131a0184aed72d94f1748e28752c4dd0d2164f2618`; it becomes staged-product integrity evidence only after Hub verifies the actual owner-only installed-package manifest and is not installed runtime evidence. The package verification passes locally; the final candidate’s focused suite has 42 passes and its client preflights compact UTF-8 claim JSON at <=4096 bytes while mapping nonempty claim 400/415/422 text/plain responses to body-free `hub_http_error`. The pure contract suite now passes after both manifests were rebound to the current validator bytes; installed service runtime, host rows, and final matrix acceptance remain pending.

Home Assistant also has a static Compose candidate retained at `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-ha-current-review/ha-compose-static.json` (SHA-256 `56e80502e4fd22274d4a4bb03e4eb25b04227a69cb37eea72e0d05b22ae81ed3`). Compose configuration, ignore boundaries, Ruff, link and diff checks pass; the Docker daemon is unavailable, so image, Linux/runtime, UI, pairing, replacement and shutdown acceptance remain pending.

The shared receipt parser now closes the source-side I14/I15 gaps identified by the prior review: every completed matrix cell reopens and binds its stdout/stderr and command-record files, evidence digest, and complete adapter-specific source/artifact role set; every non-matrix semantic fact is a value plus a hash-bound producer observation with explicit input/output bindings, so receipt-authored Boolean success claims are rejected. The focused receipt suite is 10 tests and the full Hub interop suite now passes 176/176. The independent I1–I18 rereview of this post-correction source is recorded in `/Users/bolyki/.codex/artifacts/teslatlas-interop/2026-09-08-task10-shared-i1-i18-rereview-post-i14-i15/independent-rereview.md`: no material source residual remains in the reviewed I1–I18 scope; installed capability remains disabled until the fixed registry and host lanes are admitted.

A sibling Edge execution pass additionally supplied a bounded dispatcher-to-live-Edge result: the pinned Go 1.27.0 producer submitted the same decoded synthetic record twice, received two durable admission acknowledgements and left one encrypted pending spool record with no plaintext VIN or payload; a response-loss proxy retransmission preserved the same one-record outcome. This is recorded in the execution ledger as local Edge admission/dedup evidence only. It does not establish the vehicle wire/parser route, Hub drain, restart/slow-stop behavior, installed matrix, Docker runtime or live vehicle acceptance.

### TypeScript installed-lane continuation — 2026-09-08

Hub now owns fixed Node/browser installed-adapter entries for the reviewed TypeScript candidate. `matrix_runner/installed_registry.py` binds `typescript_node` and `typescript_browser` to fixed sibling manifest/validator paths and their current source bytes, the Hub lane source hashes, the exact-toolchain 2026.36.2 SDK archive (`03ddddf132185056d60a490bc5237b3f6213d8e212209cfe111be5e09cf0a75c`) and its 81-member package inventory. The SDK path/content witness is labeled separately as `sdk_content_witness_sha256` (`4b1a44f7d2cd0ab6c772642125358703aa25c1efe32d164a119a6fc9d858ded6`). The Hub’s expected owner-staged 0600 manifest SHA-256 is `480226f91ef676461b171b131a0184aed72d94f1748e28752c4dd0d2164f2618`; it counts as staged-product integrity evidence only after Hub verifies the actual owner-only installed-package manifest, and it does not establish installed runtime. The adapter creates owner-only root/local staged inputs, verifies the installed package through the SDK acceptance helper, writes a v2 descriptor only after the runner has built SessionInput, and records the observed client runtime before semantic admission. V2 validation now also probes and binds the matrix-pinned Node executable and version before reserving that fresh descriptor. The strict lane validates the broker challenge chain, nested proof/descriptor/invitation shapes, case raw evidence, and close-before-ack cleanup. `installed_service_runtime` remains a runner-owned case: its admission result is consumed only when the shared supervisor has also accepted the complete cell.

The Hub source gate is local only: Python compilation, Node syntax, 28 Node lane tests, 132 installed-host tests, 176 Hub interop tests, and 66 companion bootstrap tests across five files pass. The TypeScript contract source gate now passes 6/6 after the owner rebound both manifest/validator hashes, and the focused Hub installed/registry checks pass 11/11. No TypeScript installed cell was run on macOS or Debian, no Docker or production broker was used, and the fixed matrix remains incomplete until actual runtime identities and clean final-stop receipts are available. Hub’s canonical current profile is now Protocol `hub-http-v1@1.0.0` SHA-256 `b80d940e8edd15896c797f659dd76e08c8b2cf2229e8386d96342b1fa4c7d926`, which matches the implemented 4,096-byte request bound and bounded extractor text responses. Both TypeScript and Home Assistant have copied the canonical `b80d…` profile, and Home Assistant’s handoff is recorded at `5cd4c83f7488cbcd66baa00941a1475dd4bd6f8dade650cb6b7a741bd59de29d`. The current TypeScript pure-validator files hash to `600e1cc3…` and `784d1618…`, and their manifests now bind those current bytes. The exact source/archive bindings and current status are indexed in `docs/compatibility/execution-state.json` under `task10_typescript_installed_lane_2026_09_08`.

The installed-lane integrity correction keeps `installed_service_runtime` pending during adapter admission and promotes it only after the runner’s independent stopped/cleanup proof; the Node/browser lane also requires runner proof service mode to equal the expected installed mode. Matrix `runtime_actual` now comes from the fixed adapter’s observed runtime inventory rather than a copy of expected job metadata. These changes remain source-only until installed execution and final matrix admission.

## Authority and ownership

- Execution may edit only the authorized Hub-owned paths in the existing checkout and must preserve unrelated dirty changes. No branches, worktrees, resets, cleans or stashes; no commits, pushes, releases, CI, artifact uploads, service operations, vehicle actions or live deployment without separate authorization.
- Future execution uses existing independent `main` checkouts, preserving unrelated edits. No branches, worktrees, resets, cleans or stashes. Reinspect HEAD and dirty files because other product tasks may progress independently.
- Hub owns its repository, Hub-side integration adapters and coordination. Six sibling owners edit their own repositories. Hub coordinates workspace authority/shared documentation and workspace-level AGENTS updates during future execution; those files are not planning outputs.
- Teslatlas App is outside development scope. Do not edit any `app/AGENTS.md` or nested App agent instructions. Do not change model defaults or API settings.
- GitHub stores source and docs only. No CI, releases, tags, binaries, image registry pushes or artifact uploads. The reviewed final source-only publication phase remains within the existing execution scope and must satisfy the ordered plan gates; a prior source-only turn restriction does not erase it.
- Do not search `vendor/`, `upstream/`, `repository-roots/`, `fixtures/teslamate-postgres/` or `TeslatlasCore.xcframework/`.
- Keep the quarantined ARM guest intact. Neither use nor recovery is allowed without a separately reviewed recovery plan. Prefer a fresh isolated guest once execution and host preparation are authorized.
- Keep secret values, vehicle routes, raw backups and private receipts outside Git. Existing private resource configuration is reused rather than requested again. Real vehicle commands, waking vehicles, production changes and receiver registration are not implicit acceptance actions.

## Current evidence and remaining gaps

Inspected on 2026-09-08. Hub HEAD was `7fe8cb2`. The landing README is `.github/README.md` (there is no root README.md); it describes native Apple-silicon macOS 13+ and Debian 13 ARM64/x86-64. These support claims must be reconciled with final evidence, not treated as proof.

| Area | Existing evidence | Remaining limitation |
| --- | --- | --- |
| Product/contracts | Ledger marks Tasks 1–9 complete; product candidate is 2026.36.2, distinct from wire/schema versions. Source exposes discovery, pairing, rotation, vehicles/current/drives, sync manifests/packs and readiness routes. | Final cohort acceptance is unfinished; preserve historical profile validators and confirm actual final source identities. |
| Native/process evidence | Recorded native and Linux profile passes, preliminary Mac package build and eight installed Mac lifecycle checks; earlier Debian service receipts exist. | Those are historical bounded snapshots, not acceptance of today's dirty tree or the entire client matrix. |
| Controller | The current shared correction has source tests and an independent I1-I18 rereview with no material source residual in its reviewed scope. `InstalledSession` snapshots, enclosing deadlines, process-generation cleanup and stopped-runtime admission are implemented. | No installed cell has exercised the complete controller/runtime path; source review remains distinct from installed acceptance. |
| Installed preparation | `prepare.py` root-owned payload hashing correction has independent C0/I0/M0 approval in `task-10-arm64-root-payload-hash-fix-review.md`. | Fresh-guest runtime retry pending. Original ARM installation failed reading root:teslatlas 0640 config as login user and remains quarantined. Do not relax permissions. |
| Shared integration | `matrix_runner/installed.py`, `adapter_wire.py`, `docker_pipe.py`, `process_generation.py`, `receipt_validation.py` and the TypeScript lane form the reviewed source base. Parent session/running authority is available to adapter launch callbacks. | Only TypeScript Node/browser are in the fixed registry. Other adapter modules and all real orchestration/cleanup runs remain pending. |
| Shared integration review | `task-10-shared-installed-integration-review.md` dated 2026-09-08 rejects the reviewed source with **C0/I18/M0**; the correction-1 and follow-up correction-2 reports index bounded source/test changes. The current I16 challenge-chain correction has 28 Node tests (including 9 strict installed-lane tests), 22 Python session tests and 176 Hub interop tests passing. The post-I14/I15 source correction adds reopened execution-log/evidence bindings, complete row identity role sets and hash-bound producer observations; its independent I1–I18 rereview passes with no material source residual in the reviewed scope. | The fixed registry now contains only the reviewed Hub-owned TypeScript Node/browser source entries; installed host rows, Docker runtime, sibling adapters, live-service evidence and final matrix acceptance remain pending. |
| Protocol | The current dirty sibling tree now contains the v2 `tools/matrix-contract.json`, pure validator, raw schema and v2 adapter tests; the earlier ledger audit that called it v1-only predates those files. | Hub has not independently bound the current bytes or supplied a fixed Protocol registry entry; installed rows remain zero. |
| Swift | The refreshed independent review binds the 25-case/two-actor contract and closes I1-I5/I7-I9 at source level, with installed/root residuals for I3/I6/I9. Hub now binds the reviewed contract/imports, fixed target/broker topology, runner observations, coordinator capability and selected-product inventory. | `swift_installed.py` still lacks SessionInput staging, actual XCTest/docker-exec workers, launch lifecycle, admission/supplement/log/runtime hooks and its fixed registry row. |
| Home Assistant | The current owner handoff binds the 18-case/two-actor contract, four raw schemas, 31-member integration, G1/G1/G2 rule, final close barrier and current profile; 115 tests pass with 2 live skips and Ruff passes. | Hub source-fixed staging/launch/admission callbacks and registry entry are absent; installed Python/HA runtime, UI and transport cleanup remain unexecuted. |
| Viewer | The current sibling tree contains a 24-case/two-actor contract, pure validator, two raw schemas and `matrix-live.mjs`, bound to the final 81-member TypeScript SDK candidate. | Hub has not bound a Viewer fixed entry. The current lane still reports pending semantic placeholders outside its narrow smoke, so source composition and installed UI acceptance remain open. |
| Edge | The owner handoff now supplies the reviewed 16-case/six-actor/31-phase contract, coordinator and six raw schemas; bounded local admission/dedup evidence also exists. | Hub still lacks the root-owned four-lane fixture, launch inventory, fixed entry and installed fault/restart/vehicle-wire proof. |
| Companion bootstrap | Task 10a standalone/Hub integration approved; existing CLI/source bootstrap present; current source regression gate passes 66 tests across five files. | Final installed matrix pending; `tools/companions/catalog-current.json` contains zero cohorts. Public acquisition is unavailable; local-candidate acceptance must be explicit. |
| Real data and completion | Tasks 11 and 12 remain pending; publication_performed=false. | No current real-history/passive Fleet/final upgrade completion claim. |
| Docker | Core `Dockerfile`, Compose, volume helper and static packaging checks are present; the native private Fleet/public-TLS listener correction is covered by Rust tests. | Docker daemon/runtime is unavailable in the current host, so image build, container health/readiness, persistence, upgrade/restore and ecosystem-container acceptance remain pending. |
| Final metadata | All six sibling `compatibility/hub.json` files are still candidate records with empty tested-version/source sets; Protocol's profile hash is null. | Scoped implementation approvals cannot populate final support claims without bound results. Owners must reconcile these manifests in final documentation work. |
| Remaining package prerequisites | Ledger records current AMD64 package assembly/expanded checks with review pending, and current Mac package approval bounded to its named snapshot. | Reuse the retained material, close pending package review, and rebuild only inputs invalidated by subsequent fixes. Do not interpret an older in-progress AMD64 build entry as stronger evidence than its later built/package-pending entry. |
| Upgrade predecessors | The previous ecosystem plan records original 2026.36.1 packages already installed, paired and stopped on three isolated hosts, with private 60-table/config/identity baselines. | Their current state was not operated or reverified here. Preserve them as upgrade references; do not replace them with a fresh source reconstruction or repurpose them as browser/matrix hosts. |

Dirty baseline includes `docs/compatibility/execution-state.json`, installed-host preparation/session/transport/tests, matrix adapter/orchestration/tests and untracked plans/process-generation files. Preserve and review them individually; do not discard them as scaffolding. Read current reports before treating older findings as still reproducible or already fixed.

Source review, retained local results, installed acceptance, passive real-data acceptance and public availability are separate evidence categories. The ledger is an index and may lag a dated rejection report. Compare the reviewed snapshot with the current diff before deciding what remains to fix; do not claim all reviewed defects necessarily reproduce unchanged today.

## Working-product acceptance criteria

- [ ] A new operator can install a locally built Hub on an isolated supported macOS host and Debian ARM64/amd64 hosts, configure it and reach truthful health/readiness without development-tree dependencies.
- [ ] Discovery returns the supported profile and identity; trusted TLS pairing, claim, rotation, expiry and revocation work through real consumers. Invalid credentials and origins fail without leaking secrets.
- [ ] Vehicles/current/drives and sync manifests/packs preserve IDs, nulls, zeros, ordering, pagination, schema and store identity across restart and supported upgrade. Unsupported capabilities are explicit; no fabricated fallback data.
- [ ] Repeated start/stop and SIGTERM are bounded, release endpoints and instance locks, and leave durable storage consistent. Service manager state, HTTP readiness and stored observations agree; an idle vehicle alone does not imply failure.
- [ ] Store/configuration/installation identity/pairings survive upgrade and restart. A tested backup/restore route handles incompatible downgrades without destructive experimentation on original data.
- [ ] Data recovery follows the actual authority boundary: `backup`/`restore-data` preserve supported historical content and installation identity but strip pairing challenges, device bearers and collector leases. Credential keys/config/TLS require separate recovery. Re-pair after restore; do not require old device tokens to revive. Upgrade and restore are different acceptance cases.
- [ ] One verified companion source bootstrap supports acquisition/build/activation/status/offline reuse/rollback. Empty catalog and incompatible/tampered sources fail clearly, preserving the active installation; local candidates are distinctly labelled.
- [ ] Actual TypeScript Node/browser, Swift, Viewer, HA and Edge consumers pass the required existing matrix on applicable installed hosts; missing required rows remain blockers.
- [ ] Real history import and passive Fleet/Edge observations are reported independently of synthetic/network/package evidence, with no unauthorized production changes.
- [ ] Final review has no unresolved blocking findings. Documentation names verified modes and remaining limitations accurately.

## Dependency map and completion boundaries

| Owner | Required input to Hub | Hub output / gate |
| --- | --- | --- |
| Protocol | Current v2 manifest/validator/raw schema and adapter bytes are present in the sibling checkout. | Verify exact owner bytes; add the Hub-fixed Protocol callbacks/registry row; then run profile conformance on all three Hub targets. |
| TypeScript SDK | Final 81-member `2026.36.2` archive and reviewed Node/browser contracts are bound. | Preserve the two fixed rows and run them on all targets with trusted-TLS/CORS/browser evidence. |
| Swift SDK | Current reviewed manifest/validator/raw schema/coordinator/worker source bundle is present. | Finish Hub root-owned staging and fixed macOS XCTest/Linux docker-exec workers; bind native URLSession/libcurl runtime and cleanup evidence. |
| Viewer | Current 24-case/two-actor contract/coordinator and final SDK/package inputs are present. | Add the Hub-fixed entry, close source composition placeholders, and run the installed browser/UI route without a development server. |
| Home Assistant | Current handoff supplies source/component/profile identities and launcher/barrier rules. | Add the Hub-fixed entry, then prove the pinned Linux HA runtime, polling/reauth/unload and preserved configuration. |
| Edge | Current handoff supplies the 16-case/six-actor/31-phase contract and coordinator. | Build the reviewed root-owned fixture and fixed entry; prove transactional commit-before-ACK, source exclusivity and real fault/restart rows. |
| Hub | Reviewed controller/runner, native packages, source/bootstrap and lifecycle | One serial writer for shared Hub files, host preparation, aggregate acceptance, workspace/docs coordination |

Parallel work is limited to non-overlapping ownership. Shared Hub controller/runner/provenance files remain serially owned. Planning and coordination follow `gpt-5.6-terra`/`max`; every Hub implementation/review/development worker follows `gpt-5.6-terra`/`high`.

**Order:** Task 1 closes the controller seam; Tasks 2–4 finish actual Hub behavior and bootstrap; Task 5 closes shared integration and sibling adapter admission; Task 6 runs native installed acceptance. The four final phases follow in their requested order. A blocked sibling row does not prevent independent Hub fixes or local verification, but it prevents claiming the complete native ecosystem gate.

**Three distinct milestones:** (a) native working candidate with all mandatory installed/cohort requirements; (b) Docker and documentation verified against local candidates; (c) source publication and reachable public companion cohort. Public Git acquisition cannot be required before the final source-update phase makes exact sibling commits reachable. Local-candidate proof is the pre-publication gate. An empty public catalog must remain visible and is not full public-availability success.

**Real-data boundary:** Real-history and passive Fleet/Edge checks are carried out within prior authority and available resources. A missing backup/credential, unavailable authorized ingress or no passive event leaves that lane pending. Report it separately; it neither proves failure nor permits waking a vehicle. All 21 installed synthetic-data cells remain mandatory regardless. Do not silently redefine the original whole-ecosystem objective as complete while a requested real-data condition remains outstanding.

## Exact native acceptance matrix

The checked `docs/compatibility/matrix.json` requires the following **21 cells**. The counts are case slots per cell, not unit-test counts or proof of execution. At the inspected revision there are 444 required case slots plus the independent cohort requirements below. Recalculate from the authoritative matrix if a reviewed contract changes; never remove rows to accommodate a missing host.

| Client / case slots per target | macOS ARM64 LaunchAgent | Debian 13 AMD64 systemd | Debian 13 ARM64 systemd |
| --- | --- | --- | --- |
| Protocol actual adapter / 21 | Required | Required | Required |
| Packed TypeScript Node / 21 | Required | Required | Required |
| Packed TypeScript browser / 23 | Required | Required | Required |
| Built Viewer / 24 | Required | Required | Required |
| Swift / 25 | Required | Required | Required |
| Installed Home Assistant / 18 | Required | Required | Required |
| Edge v2 / 16 | Required | Required | Required |

Client OS is independent of the Hub target: HA and Edge run on supported Linux targets even when Hub is on macOS. Swift needs both native macOS and Linux transport sublanes. Record browser engine/version and native versus emulated execution honestly; QEMU user-mode binary probes cannot establish systemd acceptance, and a full guest VM is not a physical native-host result.

Additional cohort requirements already in the matrix are `hub_signed_manifest`, `hub_pack_hash`, `hub_range_resume`, `hub_pack_noop`, `hub_schema_2_2_regression`, `fresh_install`, `same_input_noop`, `compatible_update`, `interrupted_update_rollback` and `clean_service_stop`. Keep Hub-side signed-pack regression for App consumers; this does not authorize App source changes or physical-device claims.

## Planning audit corrections

| Gap in the first plan | Required correction in this revision |
| --- | --- |
| Shared runner only called “in progress” | Record the accepted I1-I18 source correction, the two TypeScript fixed entries, the partial Swift seam and exact remaining registry/runtime work in Task 5. |
| Generic “all clients” acceptance | Exact 21 cells, 444 case slots and ten separate cohort requirements |
| HTTP failures conflated with client preflight | Preserve adapter-specific wrong-cursor, expiry and reauth behavior in Task 2/5 |
| Fleet listener change deferred beyond core gate | Native Fleet/public-TLS prerequisite in Task 3B; Docker reuses it rather than implements it again |
| Bootstrap public availability required too early | Local-candidate acceptance before final publication; exact reachable cohort afterward |
| Upgrade and restore loosely grouped | Pairing preservation on upgrade, deliberate re-pairing after data recovery |
| Existing upgrade preparations not named | Reuse preserved original package/baseline evidence, separate from fresh acceptance hosts |
| Test invocation ignored package layout | Explicit `-t tools/interop` for the installed-host package, named namespace-package matrix modules, and existing real entrypoint regressions |
| Collector behavior under-specified | Separate Legacy/Fleet/Edge collection, freshness, failure and recovery checks |
| Docker health/recovery oversimplified | No restart-on-idle assumption, explicit setup/network/secret ownership and verified restore |
| Final source/docs changes could stale evidence | Targeted rebuild/reacceptance for affected runtime/package inputs and final identity reconciliation |
| “All AGENTS” could rewrite frozen inputs | Active-file coverage across seven roots; classify historical generated copies without altering them or App |

## Core implementation sequence

All paths below are Hub-relative unless explicitly labelled workspace/sibling. This checkpoint update is planning-only; execution resumes from the durable goal above. For Rust changes use required `cargo check --locked`, `cargo test --locked`, `cargo clippy --locked --all-targets`, and the existing ecosystem `cargo fmt --check` gate with the supported toolchain. Formatting failures outside task ownership are reported and scoped, not bulk-fixed. Run targeted regressions first; repeat broader gates only for relevant changes or unresolved concerns. The original audit listed these as future validation instructions; the execution update above records the checks already run. A files list identifies likely seams, not permission to rewrite all of them.

### 1. Reconcile the current changes and close the controller review

**Files:** `tools/interop/installed_hosts/{session.py,transport.py,prepare.py,test_session.py,test_composition.py,test_correction.py}`, `tools/interop/matrix_runner/{installed.py,adapter_wire.py,process_generation.py,test_shared_correction.py}`, `docs/compatibility/execution-state.json`.

- [ ] Capture current branch, HEAD and task-owned diff; compare against existing reports. Record only source identities and relevant output needed to reproduce conclusions.
- [ ] Obtain independent review of operation snapshots/deadlines: detached copies, secret-free public facts, bounded private results and capacity, causal operation binding, lock timeout, open failure, cancellation and cleanup within one deadline.
- [ ] Check the actual interfaces `InstalledSession.open(..., deadline=None)`, `operation_records_snapshot(deadline)`, observation snapshots and `close(deadline=...)`. Current limits include greeting 10s, verify 30s, stop/start 60s, pair/revoke 160s and cleanup 45s; preserve their separate operation/cleanup roles and do not restart the overall cell clock at each stage. Review the 512-operation cap and capacity failure behavior before treating it as accepted.
- [ ] Fix only review findings. Reuse `test_open_passes_the_enclosing_deadline_to_transport`, `test_operation_record_snapshot_retains_processed_public_facts_without_secrets`, `test_failed_dispatched_operation_has_a_redacted_public_record` and cleanup tests already present. Add only missing blocked-lock/timeout/capacity/partial-write or failure cases. Preserve pair/revoke generation transitions and actual processed guest sequences; do not weaken admission or invent journal rows to satisfy fixtures.
- [ ] Run the following from `hub/`, retaining actual totals separately from the handed-off 128. Close review against the resulting diff; tests already passing in a recorded source snapshot need rerunning only when affected.

  ```sh
  python3 -B -m unittest discover -s tools/interop/installed_hosts -t tools/interop -p 'test_*.py'
  PYTHONPATH=.. python3 -B -m unittest hub.tools.interop.matrix_runner.test_shared_correction
  ```

  The matrix-runner directory currently has no `__init__.py`; from `hub/`, put the workspace parent on `PYTHONPATH` and use the full `hub.tools.interop...` namespace so the tests' package-relative imports and patch targets resolve. These commands were rerun against the current source: 132 installed-host tests and 24 shared-correction tests passed. Actual host execution and installed acceptance remain part of the I1 correction gate.
- [ ] Reuse the approved protected-payload observation correction. A later fresh-guest preparation must prove correct package/config ownership without displaying payload contents. Preserve the failed guest and failure records.

**Exit:** Controller changes independently reviewed and focused tests pass; installed status remains pending until Task 6.

### 2. Finish discovery, authentication, query and sync behavior

**Files:** `src/api/{server.rs,public_query.rs}`, `src/api/server/{tests.rs,cors_tests.rs,browser_cors.rs}`, `src/application/main/pairing.rs`, `src/sync/updates_delivery.rs`, `src/sync/hub_pack/`, `src/storage/db/{live_sync.rs,access_models.rs}`, `tests/{tls_import_e2e.rs,interop_fixture.rs}`, `tests/interop/`.

- [ ] Compare real route behavior with `docs/guides/api.md` and the current protocol profile; fix only demonstrated inconsistencies. Keep calendar product version separate from profile/schema/bridge versions.
- [ ] Exercise discovery, valid/expired/reused invitation, bad secret reaching HTTP 401, rotate/revoke and restart persistence through the existing real-network tests. Check wrong TLS pin and disallowed browser origin.
- [ ] Use the existing two-vehicle fixture, page size two and tied timestamps to verify current values, stable drive ordering, terminal pagination, null/zero distinctions and unsupported resources. Verify sync pack content/hash and no-op behavior across reopen and incremental update.
- [ ] Verify drive ETag/304 retains the client's accepted page, terminal cursors do not loop, and wrong vehicle/filter cursors cannot cross data scopes. At the HTTP server boundary these malformed cursor cases are real HTTP 400. Protocol and Swift intentionally exercise that request; TypeScript retains its reviewed zero-request typed rejection. Use per-adapter predicates instead of making one global rule fit all consumers.
- [ ] Keep HA's actual expired-claim 401, unknown-vehicle 404-to-empty-state mapping and credential-loss reauth distinct from SDK preflight and explicit rotation APIs. HA needs bounded polling, no SSE, and unload; do not add unused drive or rotation APIs merely for the matrix.
- [ ] Explicitly retain signed-manifest verification, pack object/hash integrity, Range resume, schema-2.2 no-op and interrupted incremental delivery. Discovery should describe actual fields/capabilities without invented ETags, epochs or richer-profile headers. Distinguish a profile describing pack routes from a client implementing pack import.
- [x] Run targeted `cargo test --locked --test tls_import_e2e` and `cargo test --locked --test interop_fixture`, plus affected query/sync unit cases and required Rust gates. Current run: TLS import 2/2, interop fixture 2/2, Hub pack 43/43 with one ignored fixture writer, updates delivery 9/9; the full Rust gates are recorded above.

**Dependency:** Protocol owner supplies reviewed raw profile/schema expectations; SDK owners supply current-client behavior. Hub fixes its server; siblings fix their own adapters. No App modifications.

**Exit:** Existing supported routes are confirmed against raw responses; identified mismatches fixed with focused regressions. No expansion of the public API is needed merely to declare the product working.

### 3. Finish native lifecycle and data preservation

**Files:** `src/application/main/{dispatch.rs,migration.rs,macos_service.rs}`, `src/runtime/{config.rs,lifecycle.rs}`, `src/storage/db/{store_opening.rs,migrations.rs}`, `src/storage/data_recovery.rs` and its tests; `macos/TeslatlasHubApp/TeslatlasHubApp/HubController.swift`, `macos/TeslatlasHubApp/TeslatlasHubAppTests/{HubControllerTests.swift,OnboardingHubControllerTests.swift,FleetOnboardingRecoveryTests.swift}`; `packaging/linux/`, `scripts/{build-macos-app.sh,build-deb.sh}`. Listener/collection seams for substeps are listed below.

**3A — Service lifecycle, native setup and recovery**

- [ ] Reproduce any remaining start/stop/onboarding defect using the normal CLI or AppKit flow. Keep live status/readiness checks live-safe; reserve immutable/offline doctor checks for stopped stores.
- [ ] Confirm the locally built package/setup route works for a second operator, including required Rust, Go/Xcode and legal inputs. Resolve any developer-machine-specific prerequisite with a focused supported setup path before native acceptance; the final documentation phase records and rechecks that path rather than discovering a new architecture requirement.
- [ ] Verify duplicate-writer rejection, finite stop/restart, cancellation during startup, port conflict, service failure reporting and recovery. Use existing supervision; no replacement process manager.
- [ ] Rehearse fresh install and upgrade on isolated stores. Compare content, installation UUID, pairings and configuration before/after; inject interruption into an affected migration only when its behavior is unresolved.
- [ ] Exercise supported backup/restore with separately protected credentials and a compatible restore target. A failed restore must preserve the original data and give an actionable error.
- [ ] Use the real `backup`, `verify-backup`, `restore-data`, `export-recovery-credentials` and `restore-recovery-credentials` CLI paths. Restore into a fresh destination; verify corrupt input, wrong recovery key, mismatched installation ID and an existing secrets directory are rejected without overwriting the source. Verify stripped pairing/collector authority, then explicit credential recovery and re-pairing. Do not impose upgrade-style bearer preservation on restore.
- [ ] Run affected Rust checks, `./scripts/test-macos-appkit.sh` (or its existing focused variant during a focused correction), `./scripts/test-macos-packaging.sh` and `./scripts/test-linux-packaging.sh` on their supported hosts. The AppKit script already selects `/Applications/Xcode-beta.app/Contents/Developer` by default. A script's packaging assertions still require later installed LaunchAgent/systemd proof.

**3B — Native Fleet listener prerequisite, reused by Docker**

**Files:** `src/runtime/config.rs`, `src/runtime/config/tests.rs`, `src/api/server.rs`, `src/api/server/tests.rs`, and existing `src/application/main/dispatch.rs` supervision only where needed.

- [x] Reproduce configuration rejection for direct Fleet plus a public HTTPS listener. Current config requires the fixed `127.0.0.1:8080` bind for Fleet telemetry while server routing merges private/public handlers. This is a native remote-client limitation as well as a Docker prerequisite; the regression is retained in the Rust configuration tests.
- [x] Make the smallest listener change from Docker plan Task 1 as a core product correction: preserve valid combined plaintext loopback configurations, add private fixed loopback ingestion for Fleet/public-TLS mode, and omit ingestion from the public router. Keep the receiver's existing fixed endpoint/patch and public wire profile unchanged. A TLS listener cannot also occupy the receiver's plaintext `127.0.0.1:8080`; reject that collision with an actionable configuration change. Cover public TLS bound to another loopback port as well as a LAN address.
- [x] Verify old loopback config, public TLS Fleet config, non-loopback plaintext rejection, non-loopback proxy rejection, listener collision, bind failure and cancellation. Private ingestion needs its token; a public ingestion request must not reach the handler even with that token. Both listeners must stop and release ports before orderly shutdown is accepted. These are source and local process tests; installed/live acceptance remains pending.

**3C — Collection health, durability and recovery**

**Files:** Existing `src/collection/collector/{authentication.rs,streaming.rs,scheduler.rs,fleet_supervision.rs,owner_supervision.rs}`, `src/collection/{fleet_telemetry.rs,edge_delivery.rs}`, their existing tests, `src/storage/db/observation_persistence.rs` and relevant `src/api/server.rs` readiness logic.

- [ ] Reuse fake-Tesla/synthetic transport tests to confirm Legacy polling/stream reconnect, bounded retry/backoff, credential-expiry/refresh failure and recovery without duplicate ingestion or a second refresh owner. Keep real Tesla egress disabled in these fixtures.
- [ ] For direct Fleet, feed the reviewed receiver/ingress path synthetic envelopes and confirm commit before successful response, retry after persistence failure, and a newer durable observation visible to queries/sync. For Edge, preserve the distinct v2 spool/pull/commit/ACK path and exclusive selected source; do not conflate the two receiver bridges.
- [ ] Check `/healthz` means a responding process and `/readyz` reports the actual serving conditions; neither is evidence of a fresh vehicle event. Represent offline/auth-failed/stale versus sleeping/idle states accurately in status/AppKit and logs, with no credentials in diagnostics.

**Exit:** Operator start/stop/setup, authorized collection modes and data recovery each have specific local proof. Native Fleet remote access no longer waits for Docker development. Installed acceptance and passive real traffic remain Task 6 obligations.

### 4. Finish companion bootstrap using the existing implementation

**Files:** `src/application/main/companions.rs`, `scripts/bootstrap-companions.py`, `tools/companions/` and its tests/catalog, `docs/compatibility/ecosystem-release.json`.

- [ ] Verify existing source acquisition resolves to immutable, Hub-compatible sources and dependencies. Reuse current recipes; do not introduce separate companion Mac installers, embedded sibling payloads or idle SDK/protocol services.
- [ ] Test candidate install, exact offline reuse, status and rollback; reject tampering, missing source, interrupted activation and incompatible profile while preserving previous active output.
- [ ] Exercise the packaged Hub wrapper without the source tree using the existing tests and one installed smoke. Keep public catalog admission separate from local-candidate success.
- [ ] Coordinate missing output/interface corrections with sibling owners; admit no invented public cohort merely to produce a successful demo.
- [ ] Preserve the actual catalog semantics: `install` uses verified cached/shipped selection; `update` alone refreshes the fixed Hub GitHub source catalog with its existing 15s/1 MiB bounds. `status` can recover an interrupted transaction and therefore is not universally read-only; `dry-run` must not mutate the installation. Wrong source/tool/version/profile must fail before activation; any recovery work must respect prior active state.
- [ ] Verify the SDK-before-Viewer dependency and exact package hash, protocol source-tooling output, Swift source/release products, HA immutable integration link and Edge Linux target. HA data/registry and Edge spool/config are not bootstrap rollback payloads. No bootstrap command starts services or registers Tesla telemetry.
- [ ] Keep local manifests, installation prefixes and artifact results private. For any required new toolchain/host identity, fix that setup narrowly under execution authority; do not relax source/secret admission or create another validator framework. Use `tools/companions/tests/` and the existing compiled wrapper tests, followed by the source-unavailable packaged wrapper check.

**Exit:** Supported bootstrap path is repeatable; empty public catalog remains documented until a real eligible source cohort exists.

### 5. Close specific ecosystem integration blockers

**Files:** `tools/interop/{run.py,test_run.py}`, `tools/interop/matrix_runner/{installed.py,installed_registry.py,adapter_wire.py,docker_pipe.py,swift_entrypoint.py,process_generation.py,receipt_validation.py}`, relevant schemas/tests; `tools/interop/client_lanes/{run.mjs,installed_contract.mjs}` and its existing tests; `docs/compatibility/{matrix.json,receipt.schema.json,execution-state.json}`. Consume accepted source-evidence helper interfaces; do not replace them. Edge fixture/controller changes follow its reviewed design with explicit ownership after controller review.

**5A — Close shared integration I1–I18 as the existing correction batch**

Read the complete `task-10-shared-installed-integration-review.md` and `task-10-shared-installed-integration-fix-1-brief.md`. The table below groups the required work for planning; each of the 18 original findings still needs an individual disposition and rereview. These are Hub integration defects, not grounds for rewriting sibling production clients.

| Findings | Concrete correction / meaningful validation |
| --- | --- |
| I1, I2, I3 | Real `run.py` package imports and v2 dispatch; separate installed normalization from v1; actual Node/browser `run.mjs` installed branch. Exercise those entrypoints rather than only imported helpers. |
| I4, I5, I6 | HA initial advance then final barrier; immutable root controller/runtime views; actual operation-result/journal/stop bindings. Test real controller methods with synthetic OS boundaries; do not invent verify result rows or hardcode initial guest sequence 1. |
| I7, I8 | Full staged SessionInput provenance and fresh outputs outside source/export roots; descriptor-based bounded owner/type/no-follow/stability checks before hashing. FIFO, oversize, foreign-owner and path-swap inputs must reject without blocking or reading arbitrary bytes. |
| I9, I10, I11 | Immediate independent cleanup ownership; terminate the whole owned process generation even after leader exit; one enclosing execution deadline plus bounded cleanup; nonblocking Docker bridge with capped stderr. Test early admission failure, nonreading child and descendant-held pipe with real local subprocesses. |
| I12, I13 | Execute admitted module bytes with correct registration and no unbound bytecode; Swift snapshots reflect completed worker operations, reject foreign sessions and remain immutable. Use real owner dataclass/module shapes and both fixed worker entries. |
| I14, I15 | Derive matrix and non-matrix gate acceptance from exact producer/cohort/runtime/command/data facts. Reject asserted completeness, fabricated case dictionaries and comparator hashes not bound to before/after inputs. Keep original failed attempts and authentic accepted evidence distinct. |
| I16, I17, I18 | Strict duplicate-free UTF-8/typed wire and permanent failure latch; supplement rejection becomes a redacted failed row while preserving prior rows; complete accepted source/export/output checks immediately before/after each row. |

- [x] Compare current corrections with every finding before editing; retain existing useful `test_shared_correction.py` cases. Add only the missing source-faithful regressions, then rerun affected suites. No real Docker/SSH/installed execution is implied by a synthetic test pass.
- [x] Independently review the complete coherent correction. The post-I14/I15 independent I1-I18 rereview reports no material source residual in its reviewed scope; the production registry admits only the reviewed Hub-owned TypeScript Node/browser source entries, while every other adapter remains disabled until its specific contract/source/launch inventory is approved. Runtime job JSON cannot select new code or bypass the registry.

**5B — Admit approved sibling adapters and prepare Edge's specific fixture**

- [ ] Complete `swift_installed.py` through TDD. Add a closed `build_session_input(...)` that stages the selected Swift archive, installed-product inventory, canonical profile, scenario and certificate into distinct owner-only root/local paths; emits exactly `swift_macos` then `swift_linux`; binds the runner descriptor/session/deadline; and reserves fresh normalized, actor, coordination and framework-log outputs outside every source/product root. Test foreign artifacts, wrong member inventories, path swaps, extra actors and broker/topology changes.
- [ ] Add fixed Swift worker launchers. The macOS worker runs the reviewed XCTest `TeslatlasCurrentHubTests.CurrentHubMatrixWorkerTests/testInstalledCurrentHubMatrixWorker`; the Linux worker uses the existing bounded `docker_exec_pipe` transport with the same reviewed test and a private `TESLATLAS_CURRENT_HUB_MATRIX_WORKER_CONFIG`. The root coordinator alone derives worker configuration, phase files and runtime identity. It must terminate both owned process generations and remove private staging after success, rejection, timeout or exception.
- [ ] Implement Swift `admit`, `build_supplement`, `execution_logs` and `runtime_inventory`, then add the `swift` row only when `_entry(...)` accepts every callable and the exact three-target local/docker execution plus Unix-broker topology. Preserve the installed/root residual gates for real wrong-secret HTTP 401, cursor/body provenance and exceptional cleanup; a source-green registry row remains unaccepted at matrix level.
- [ ] Bind the current Protocol v2 manifest, validator, raw schema and adapter into a Hub-owned fixed module and registry row. Re-run the owner source gate and reject any job-supplied executable, validator, argv, output or broker authority. Treat the older v1-only ledger audit as historical, not as a present missing-owner blocker.
- [ ] Bind the current HA handoff into a Hub-owned fixed module and registry row. Stage the exact 31-member component and 17-member canonical profile, preserve its two actors, require runner-owned G1/G1/G2 observations and `close_completed`, and keep `installed_service_runtime` pending until the runner accepts final stop and transport cleanup.
- [ ] Bind the current Viewer contract/coordinator and exact 81-member SDK plus 80-member Viewer product into a Hub-owned fixed module and registry row. Execute the packaged production server and installed Playwright route; reject fixture/Vite/dev-server substitutions and any normalized case left as a semantic placeholder.
- [ ] Implement only the reviewed root-owned Edge fixture seam from `teslatlas-edge/docs/superpowers/edge-matrix-contract-handoff-2026-09-08.md`, then bind its 16-case, six-actor, 31-phase contract/coordinator in a fixed entry. Prepare normal/reference/fault/negative actors before sealing session/store identity, retain mTLS and encrypted spool, and keep this seam separate from the ordinary five-operation broker.
- [ ] Run each owner contract suite, its focused Hub module/registry tests, the complete matrix-runner suite and one isolated composed local-process lane per adapter before host expansion. Obtain an independent source review of the exact fixed registry and launch inventories. Environment/toolchain failures get targeted setup fixes, not validator or evidence exceptions.

**Exit:** Shared C0/I18 batch and participating sibling reviews closed against current source, fixed registry entries explicitly admitted, and one real installed cell completes normal final-stop/child cleanup. Partial readiness of one adapter does not approve all seven.

### 6. Practical end-to-end installed acceptance and upgrade

**Files:** Existing `tools/interop/run.py`, installed host/matrix configuration and fixtures; `docs/compatibility/{matrix.json,execution-state.json,acceptance.md}`. Private runtime inputs and raw evidence stay outside repositories.

- [ ] Use fresh isolated macOS/LaunchAgent and Debian ARM64/amd64/systemd targets. Do not install over the owner's live Mac Hub; do not touch the quarantined guest. Confirm actual architecture/runtime and required toolchain availability before long builds.
- [ ] Reuse the already prepared host/tooling records after current identity checks. Use the dedicated browser host rather than upgrade-reference hosts. Where a fresh ARM guest is required, apply the reviewed root-only payload observation fix and require successful ownership/hash verification before service acceptance. Close the outstanding current AMD64 package review.
- [ ] Build from the current reviewed candidate inputs per required target; avoid rebuilding unchanged artifacts merely for another test. Install it and follow the operator path: bootstrap/configure → start → discovery → trusted pairing → query/sync → newer synthetic observation → revoke/re-pair → stop/restart → upgrade → verify preserved content → backup/restore with re-pairing → orderly stop.
- [ ] Run actual packed Node/browser SDK, Swift worker, container/installed Viewer, HA flow and Edge runtime against those hosts according to the existing matrix. Check values and failure behavior, including unavailable/recovery, expiry, stop/reconnect, Edge retry/deduplication and ACK interruption. Report each required row, with explicit failures/skips.
- [ ] Use the existing runner's `--require-complete` path with private configuration and retained receipt output. Require real service state, endpoint closure and lock release at cleanup; no synthetic receipt substitution.
- [ ] Invoke `python3 tools/interop/run.py --config "$HUB_MATRIX_CONFIG" --require-complete --receipt "$HUB_MATRIX_RECEIPT"` from Hub after assigning those variables to the reviewed private config and new private receipt path. A complete result must contain exactly the authoritative required cells/cases, no hidden pending rows, accepted source/artifact/profile/runtime identities and the ten cohort requirements. A receipt parser or zero process exit alone is insufficient.
- [ ] Reuse preserved original 2026.36.1 packages and their verified baseline evidence for predecessor upgrades after separately admitting those environments for upgrade work. Preserve an untouched baseline/copy. Compare supported history content, configuration, installation identity and paired credentials. Test Hub-first/client-first compatibility with actual available predecessor artifacts; record never-distributed siblings as such instead of inventing old versions. Use coordinated restore for incompatible migrations; no image/binary downgrade against a newer unverified store.
- [ ] Reuse the known TeslaMate backup/private configuration for an isolated, history-only import after confirming authority and the existing safe credential-preservation path. Compare normalized content/IDs/nulls/time bounds, not only row counts. Do not refresh copied production tokens or modify VPS services.
- [ ] For passive live collection, identify a single authorized refresh owner and record starting/new durable observation watermarks. No event within the agreed window means pending. Actual Fleet/Edge provenance is a separate claim and requires its own evidence; do not wake/register/reconfigure to force it.
- [ ] For private history import, retain the unmodified consistent source backup and inspect the current migration credential path before running it. Use supported preserved-credential/history-only behavior with Tesla egress disabled; if it is insufficient, make a focused import correction with unchanged-credential and zero-egress proof. A TeslaMate/MyTeslaMate token is not a Fleet credential. Use `observation-watermark --car-id ...` followed by `verify-observation --car-id ... --watermark ...` only through the admitted configuration, and compare consumers to durable values rather than readiness alone.
- [ ] Review final installed results and upgrade/mixed-version behavior with sibling owners. Leave the aggregate incomplete for missing mandatory rows; label separately any externally blocked real-data lane.

**Evidence minimum:** One concise redacted result index reusing existing receipts: exact source/dirty input identity, artifact/profile digest, host/runtime mode, command/exit, case pass/fail/pending, independent final stop/cleanup and the before/after content comparisons. Preserve private raw data and original failed attempts. Do not rebuild the accepted evidence framework or claim historical missing streams were recovered by a rerun.

**Core gate:** Native runtime/installed/cohort criteria met for every claimed native mode and required consumer, with requested private lanes reported separately. Final guidance, product Docker packaging, complete documentation reconciliation and public source availability are deliberately the following phases; they are not circular prerequisites of this native gate. This is an intermediate milestone, not whole-objective completion. Begin the following four final phases in order.

## Final phase 1 — Short, consistent AGENTS guidance

**Files:** All applicable Hub `AGENTS.md`; workspace `AGENTS.md`/authority guidance; coverage coordination for `teslatlas-protocol`, `teslatlas-sdk-typescript`, `teslatlas-sdk-swift`, `teslatlas-viewer`, `teslatlas-home-assistant`, `teslatlas-edge`. Each sibling task edits only its own repository.

- [x] Inventory every applicable AGENTS.md across the seven non-App product roots and workspace, including nested files, respecting excluded trees. Record coverage and owner; explicitly exclude every App AGENTS file. See `task-10-agent-guidance-inventory.md`.
- [x] The planning audit found one active root AGENTS.md per non-App product plus historical copied AGENTS under Hub `dist/` staging. Classify generated output, frozen review snapshots and historical source bundles separately; preserve their recorded bytes. “All projects” covers current authoritative instructions, not a rewrite of immutable historical inputs. Reinspect for new active nested files at execution time.
- [x] Recheck the [official GPT-6 Astra guide](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra). Rechecked on 2026-09-08; its practical guidance on authorized follow-through, instruction priority, concise communication and proportionate verification remains consistent with this plan. Retain repository-specific commands and user authority.
- [x] Keep guidance short. Resolve ordinary decisions autonomously, prepare concrete work before a necessary approval, preserve session permissions, and delegate only bounded independent work when useful and authorized. Do not introduce elaborate agent machinery, mandate unnecessary testing or alter model defaults.
- [x] Reconcile existing Astra sections rather than append duplicates. Hub coordinates workspace changes and verifies sibling coverage without editing their files. Check the final scoped diffs show zero App AGENTS changes.

## Final phase 2 — Build and validate the Docker solution once

**Canonical detail:** Reuse [2026-09-08-docker-product.md](2026-09-08-docker-product.md), including its ecosystem amendment. This phase schedules that work; it is not a second Docker implementation. Its Task 1 listener prerequisite is executed once as native Task 3B above. Where the earlier plan proposes Hub edits to sibling files, ownership here prevails: sibling owners make those edits. Public docs follow in final phase 3. Product Dockerfiles/Compose/runtime packaging begins here. Existing isolated HA/Swift/browser client containers may be used earlier for native matrix acceptance under execution authority; they are acceptance infrastructure, not the new Docker product.

**Files:** `Dockerfile`, `.dockerignore`, `compose.yaml`, `packaging/docker/`, `scripts/test-docker-packaging.sh`; existing Docker plan's optional Fleet/Viewer/HA/Edge build/runtime files and `scripts/test-docker-ecosystem.sh`; focused `src/api/server.rs`/`src/runtime/config.rs` listener changes and tests.

- [ ] Build a small non-root multi-stage Hub runtime from locked source, with runtime CA libraries, required notices, immutable base identity and no compiler/secrets/database in image layers. Keep native Mac support.
- [ ] Use a Hub-only source build context; `.dockerignore` excludes `.git`, private configuration, databases, credentials, unrelated `dist`/`target` outputs and workspace material while retaining required source/legal inputs. Verify architecture/runtime dependencies from the built executable. Container version metadata comes from the selected Cargo version, never a fabricated new release/tag.
- [ ] Provide one minimal base Compose example: persistent data/config, read-only secret/TLS mounts with correct ownership, health and readiness, bounded SIGTERM shutdown and logging. Use foreground `serve`, not host supervisor commands. No Kubernetes or new orchestration service.
- [ ] Create the fixed non-root account and explicit one-time new-volume ownership setup; do not recursively chown existing user data at startup. Read-only root, dropped capabilities, no-new-privileges and writable temporary/state paths must work with actual CLI admission. Secret mounts must satisfy the existing owner/mode checks on Linux and Docker Desktop; never solve a mount issue by broadening key permissions.
- [ ] Reuse Task 3B's verified private `127.0.0.1:8080` ingress/public HTTPS separation. Recheck it from outside the container namespace. Any Docker-specific change to shared service code requires the affected native listener/lifecycle regression again before final publication.
- [ ] Optional Fleet services share Hub's network namespace to preserve loopback contracts; never publish private ingress/proxy ports. Use pinned reviewed companion builds. Viewer/HA access public TLS separately; Edge retains its distinct receiver and persistent encrypted spool. Direct Fleet and Edge remain explicit alternatives.
- [ ] Retain the earlier design's public Hub container port 8443, Fleet receiver port 8444 and private command proxy 4445; only explicitly configured public ports are published. Fleet host 443 publication belongs to the namespace-owning Hub service's optional override. Prove one-off configuration containers reach the required loopback proxy before documenting a `compose run` setup sequence; sharing a Compose network is not by itself sharing loopback.
- [ ] Reuse verified source bootstrap for all six sibling products. SDKs/protocol are build/export products, not runtime services; Viewer uses its packaged server; HA uses an explicit integration activation into isolated HA for tests; Edge keeps durable delivery state. No automatic replacement of production HA or sibling credentials.
- [ ] Resolve companion tooling explicitly: keep compilers and the six sibling builds in optional build tooling or on the supported build host, then supply only verified runtime outputs. If the runtime exposes `companions` commands, ship Python 3.10+ and the audited helper/modules/schema/catalog in a layout accepted by `resolve_helper_layout`; missing build toolchains must produce the documented error. Do not advertise an in-container install command that cannot run, or inflate the core service image with every language toolchain.
- [ ] For HA, mount the bootstrap's immutable integration target at a valid container path or use a narrowly reviewed target recipe; prove links survive container recreation and rollback. For Viewer, verify normal browser TLS/origin behavior against the container-served packaged bundle. For Edge, verify private receiver access, durable encrypted spool, certificate/identity rejection, retry and ACK faults across container recreation. Existing HA and Edge data stay outside replaceable images.
- [ ] After TLS/configuration/volume setup, the shortest startup is `docker compose up --build -d hub`; verify that exact command. Document bootstrap and credential entry through supported private-file/stdin commands first. Fleet/optional components use the earlier plan's explicit overrides, not a mandatory large stack.
- [ ] Validate the proposed sequence before it becomes operator documentation: copy/configure examples and establish certificate trust → prepare only new volumes → `docker compose build hub` → `docker compose run --rm hub --config /etc/teslatlas-hub/config.toml bootstrap` → stopped-service credential setup through supported stdin/private files → `docker compose up -d hub` → live-safe status/logs → `docker compose stop hub`. Verify CLI argument order against `src/application/main/cli.rs` and the future built binary. Initial bootstrap is not proof of collector readiness.
- [ ] Probe `/healthz` with trusted TLS and a finite timeout; report `/readyz` separately using its actual 200/503 semantics and redacted reason. Do not run doctor/preflight periodically against a live store. Set log rotation, a finite stop grace period (30s is an initial proposal to verify) and an appropriate restart policy. Compose health status alone does not automatically restart an unhealthy container, and an idle car must not trigger a restart loop.
- [ ] Test health versus readiness, trusted TLS, wrong ownership/missing secrets, duplicate writer, stop/recreate persistence, supported upgrade and stopped consistent backup/restore on Linux ARM64/amd64 and separately Docker Desktop where claimed. Never use insecure TLS probes, copy a live SQLite file blindly or suggest `down -v` as ordinary shutdown.
- [ ] Run the actual Docker Viewer/HA/Edge paths and protocol/SDK build checks with owner-supplied outputs; retain local-candidate versus public availability distinction. Public catalog admission remains a blocker for a public acquisition claim.
- [ ] Use one clean installation and one persisted upgrade/restore per claimed architecture with known expected content. Validate `docker compose config` for the base and selected overrides, then record exact image/base/source identity, Docker/Compose version, runtime architecture and executed cases. Native Linux AMD64/ARM64 Docker support requires those actual runtime lanes; QEMU compilation and Docker Desktop are separate evidence. Missing native hardware/host access is explicit pending support, not a reason to call emulation native.

**Docker recovery acceptance:** Stop the writer for the documented consistent backup operation; use supported Hub backup/verification and separate credential recovery, retaining required config/TLS/key material independently. Restore to fresh volumes, re-pair clients as required, prove history/identity and a newer synthetic observation, and retain the original volumes until acceptance. Compatible restart/upgrade retains pairings; data restore does not. Removing containers must preserve named volumes; volume deletion is a separate destructive operation.

**Networking/persistence contract:** Public Hub TLS defaults to host loopback publication; LAN use requires an explicit bind and matching trusted hostname/certificate. Browser URLs must be reachable from the user's browser, not Docker-only DNS. Persist Hub data/identity, runtime configuration and required credential recovery material across container replacement; persist Edge spool and HA config separately. Recreate the optional Fleet namespace stack coherently. No universal NAS/Windows or live Tesla acceptance claims without separate proof.

## Final phase 3 — Reconcile all current documentation

**Files:** `.github/README.md`, `.github/SUPPORT.md`, `docs/index.md`, `docs/guides/{getting-started,build-from-source,install-macos,install-debian,configuration,cli,api,companion-setup,fleet-setup,troubleshooting}.md`, new `docs/guides/{install-docker,docker-companions}.md`, `docs/operations/backup-and-recovery.md`, relevant `docs/releases/`, `docs/architecture/`, `docs/compatibility/` and development/tool READMEs. Hub coordinates workspace ecosystem docs; siblings own their docs.

- [ ] Inventory current docs and links; distinguish historical plans/reports from current operator guidance. Correct obsolete claims and broken authority links without rewriting historical evidence into success.
- [ ] Document actual prerequisites, source-only local build, shortest native/Docker startup, runtime secrets, configuration, TLS/origin rules, companion selection, supported profiles, upgrade/restore and concrete troubleshooting.
- [ ] Explain public catalog availability, candidate mode, architecture/emulation limits, core-only Debian Fleet limits and completed versus pending acceptance. Do not imply published binaries/images or validated modes that did not pass.
- [ ] Reconcile every sibling `compatibility/hub.json` through its owner: reviewed profile hash (including Protocol's currently null hash), tested Hub versions/source fingerprints, supported capabilities and redacted receipt links. Hub owns the aggregate matrix/release/execution/acceptance docs. A candidate with empty tested fields must not be relabelled supported solely because local compilation passed.
- [ ] Audit practical source-build prerequisites: current Mac packaging expects the selected Go 1.27.0 executable/path/GOROOT to match a reviewed host identity, while Debian needs architecture-compatible binaries and legal inputs. Supply the shortest supported way for another operator to satisfy these checks. If the current path is tied to a single developer machine and prevents the advertised workflow, make a focused reproducibility/setup correction with its affected checks; do not tell users an ordinary Go install suffices or replace the validation architecture.
- [ ] Check installation/uninstallation with data preservation, configuration examples, API route/field/status claims, logs/redaction, backup re-pairing, Fleet/Edge exclusivity, native limitations and developer test commands. Preserve source-neutral contracts and required license/notices; do not copy Hub implementation into differently licensed sibling contracts.
- [ ] Follow the complete installation and recovery instructions in disposable accepted environments; check links relative to `.github/README.md` and reconcile every affected current document. Reuse existing documentation checks; no new broad framework.
- [ ] Check new Docker files against existing repository layout/provenance classification. Update `provenance-manifest.json` and legal/source documentation only where their coverage changes, then use the existing `scripts/verify-repository-layout.py` and `scripts/verify-provenance.py` interfaces and affected tests. Do not run unrelated release/signing/publishing gates merely because their filenames contain “test”.
- [ ] Reuse the existing finite source/evidence finalization order from `task-10-final-integration-design.md` and the accepted helper correction. Distinguish a docs-only source identity update from a changed package/runtime input; rebuild/retest only affected outputs. Capture the final reviewed source/export binding after documentation reconciliation without self-referential receipt hashing.

## Final phase 4 — Final review and source-only GitHub update

**Files:** Only approved task-owned source/docs in Hub; workspace coordination changes remain separately attributed. Each sibling task publishes its own product-specific diff.

- [ ] Review final raw diff and meaningful test/package/installed/Docker/doc evidence. Recheck no intervening source changes invalidate the accepted result. Summarize remaining limits honestly; unresolved mandatory acceptance blocks completion.
- [ ] Verify actual Hub remote, current branch and dirty state immediately before Git writes. Stage explicit task-owned paths/hunks only; preserve unrelated changes and exclude secrets, raw evidence, packages, binaries and build outputs.
- [ ] Coordinate the final source-only dependency order with sibling owners: reviewed protocol/SDK/Edge/HA/Viewer source commits become reachable under their own publication steps, with TypeScript outputs bound before Viewer. Revalidate the already tested content against those exact immutable commits. A remote commit that includes new runtime changes needs affected acceptance again; reachability alone is not compatibility.
- [ ] Prepare the public companion catalog from the final reachable sibling commits, source digests, locks and verified output hashes. Validate it locally using the existing bootstrap's production Git-source mode before the final Hub push; use the local-candidate path for every earlier pre-publication check. The catalog's component source pins must not depend on a future self-referential Hub commit.
- [ ] Once execution is explicitly authorized and its final publication conditions are met, commit and push reviewed Hub source/docs only to the verified product repository. No CI, release/tag creation or artifact uploads. This planning run grants no Git-write permission.
- [ ] After the final source update, verify the public fixed-origin catalog selects the intended immutable cohort and exercise one unprivileged disposable public-source install/update using locally built outputs. This is final publication verification, not a registry/artifact release. If exact sibling sources/catalog are still unavailable, report source publication and public bootstrap separately; leave public availability pending rather than fabricating an admitted cohort or adding an unrequested deployment.
- [ ] Verify pushed commit identity and report scoped changes, meaningful checks, supported workflows and remaining external limitations. Do not publish sibling or App changes from Hub's Git operation.
